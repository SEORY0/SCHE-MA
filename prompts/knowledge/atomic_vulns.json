{
  "_about": "Atomic Vulnerability library — the 28 CyberGym crash types (arXiv:2506.02548 distribution). Each is an Example(V_i): a minimum-margin, single-invariant input recipe the generator instantiates against the target codebase C, so PoC = F(C, Example(V)). GLOBAL & task-agnostic (disclosed at leaderboard submission). The agent classifies a task into all-applicable type ids; only those entries are injected into Stage 3.",
  "types": {
    "heap-buffer-overflow-read": {
      "label": "Heap-buffer-overflow READ", "sanitizer": "asan",
      "aliases": ["heap-buffer-overflow read", "heap-buffer-overflow"],
      "sink": "memcpy/memmove or buf[i] READ where the index/length comes from one input field but the buffer was heap-sized from another field (or a constant).",
      "recipe": "Keep a valid base that reaches the sink; set the read offset/index = len+1 where len is the buffer's own (smaller) size field. One past — minimum margin.",
      "fp_guard": "An oversized index / huge length crashes the FIXED build too (score 0). Violate exactly ONE field by one step; keep every other byte valid."
    },
    "heap-buffer-overflow-write": {
      "label": "Heap-buffer-overflow WRITE", "sanitizer": "asan",
      "aliases": ["heap-buffer-overflow write"],
      "sink": "memcpy/strcpy/buf[i]=... WRITE into a heap allocation sized from one field while the copy length comes from another.",
      "recipe": "Source/copy length = capacity+1 (one byte past the allocation). Keep the allocation-sizing field at its honest small value.",
      "fp_guard": "Don't enlarge the allocation field too — only the length must exceed capacity by one."
    },
    "stack-buffer-overflow-read": {
      "label": "Stack-buffer-overflow READ", "sanitizer": "asan",
      "aliases": ["stack-buffer-overflow read"],
      "sink": "Read of a fixed-size local array `buf[N]` at an attacker-influenced index.",
      "recipe": "Index = N (first out-of-range). Keep the input structurally valid up to that read.",
      "fp_guard": "Index far past N or corrupt structure crashes both builds."
    },
    "stack-buffer-overflow-write": {
      "label": "Stack-buffer-overflow WRITE", "sanitizer": "asan",
      "aliases": ["stack-buffer-overflow write"],
      "sink": "Unchecked copy/loop into a fixed local array `buf[N]`.",
      "recipe": "A subfield whose decoded length is exactly N+1 bytes into a `buf[N]`. One past.",
      "fp_guard": "A wildly long field overruns into unrelated frames and crashes both builds."
    },
    "stack-buffer-underflow-read": {
      "label": "Stack-buffer-underflow READ", "sanitizer": "asan",
      "aliases": ["stack-buffer-underflow read", "stack-buffer-underflow"],
      "sink": "Read of a local array at a negative index (signed offset/seek goes below 0).",
      "recipe": "A signed offset/index field = -1 (first below the array base). Keep all else valid.",
      "fp_guard": "Large negative values may hit unmapped memory and crash both builds."
    },
    "stack-buffer-underflow-write": {
      "label": "Stack-buffer-underflow WRITE", "sanitizer": "asan",
      "aliases": ["stack-buffer-underflow write"],
      "sink": "Write to a local array at a negative index.",
      "recipe": "Signed write index/offset = -1. Minimum underflow only.",
      "fp_guard": "Only -1; deeper underflow corrupts the frame in both builds."
    },
    "global-buffer-overflow-read": {
      "label": "Global-buffer-overflow READ", "sanitizer": "asan",
      "aliases": ["global-buffer-overflow read", "global-buffer-overflow"],
      "sink": "Indexing a static/global table/array with an attacker-controlled index (READ).",
      "recipe": "Index = table_len (first out of range). Reach the lookup with a valid prefix.",
      "fp_guard": "An absurd index reads unmapped memory in both builds."
    },
    "global-buffer-overflow-write": {
      "label": "Global-buffer-overflow WRITE", "sanitizer": "asan",
      "aliases": ["global-buffer-overflow write"],
      "sink": "Writing into a static/global table at an attacker-controlled index.",
      "recipe": "Write index = table_len (first out of range).",
      "fp_guard": "One past only."
    },
    "heap-use-after-free-read": {
      "label": "Heap-use-after-free READ", "sanitizer": "asan",
      "aliases": ["heap-use-after-free read", "use-after-free read", "heap-use-after-free", "use-after-free"],
      "sink": "A read through a pointer that was freed on an earlier error/cleanup/realloc path.",
      "recipe": "Drive the error/realloc path that frees `p`, then a FOLLOWING valid record re-references the same id/handle so it is read. Both records parse; the crash is the ORDERING, not malformed bytes.",
      "fp_guard": "Don't malform the second record — it must be valid; only the free-then-reuse sequence triggers the bug (and only on the vul build)."
    },
    "heap-use-after-free-write": {
      "label": "Heap-use-after-free WRITE", "sanitizer": "asan",
      "aliases": ["heap-use-after-free write", "use-after-free write"],
      "sink": "A write through a pointer freed on an earlier path.",
      "recipe": "Same free-then-reuse ordering as UAF-read, but the reused handle is written (e.g., an update/append op on the freed object).",
      "fp_guard": "Sequence/ordering only; keep both records well-formed."
    },
    "heap-double-free": {
      "label": "Heap-double-free", "sanitizer": "asan",
      "aliases": ["heap-double-free", "double-free", "attempting double-free"],
      "sink": "free() reached twice on the same pointer (duplicate cleanup / error after partial free).",
      "recipe": "Walk the free path twice: a duplicate id / repeated end-marker / an error AFTER a partial free that triggers the cleanup free again.",
      "fp_guard": "The structure must be valid enough that the first free path runs cleanly."
    },
    "use-of-uninitialized-value": {
      "label": "Use-of-uninitialized-value", "sanitizer": "msan",
      "aliases": ["use-of-uninitialized-value", "use-of-uninitialized value", "uninitialized"],
      "sink": "MSan: a value is READ before it was written, because an optional field was skipped.",
      "recipe": "OMIT an OPTIONAL subfield that is format-legal to omit, so a downstream value is read before any code path set it. The file stays well-formed.",
      "fp_guard": "Don't truncate the file or break required fields (that's a parse error in both builds). Only leave a legitimately-optional value unset."
    },
    "wild-address-read": {
      "label": "Wild-address READ", "sanitizer": "asan",
      "aliases": ["wild-address read", "wild-address", "segv on unknown address", "unknown-crash"],
      "sink": "A read through a pointer computed from input (offset/base/index) that lands outside any valid mapping.",
      "recipe": "Set the single offset/pointer field so the computed address is just past a legitimate object (first invalid) — not a random huge value. Reach it with a valid prefix.",
      "fp_guard": "A garbage huge pointer faults in both builds; the bug is a specific off-by-one/missing-bound on ONE field."
    },
    "wild-address-write": {
      "label": "Wild-address WRITE", "sanitizer": "asan",
      "aliases": ["wild-address write"],
      "sink": "A write through an input-derived pointer that escapes valid memory.",
      "recipe": "One offset/index field set so the write target is the first invalid address past a real object.",
      "fp_guard": "Minimum margin; a wild huge address crashes both builds."
    },
    "null-dereference-read": {
      "label": "Null-dereference READ", "sanitizer": "asan",
      "aliases": ["null-dereference read", "null-dereference", "segv on unknown address 0x000000000000"],
      "sink": "A dereference with no NULL check after a failing alloc/lookup/parse returns NULL.",
      "recipe": "Drive a lookup/alloc/parse to LEGITIMATELY return NULL (e.g., a missing optional sub-object), then a valid path dereferences it without checking.",
      "fp_guard": "The NULL must come from a real, valid-input path — not from a corrupt file that fails everywhere."
    },
    "index-out-of-bounds": {
      "label": "Index-out-of-bounds", "sanitizer": "ubsan",
      "aliases": ["index-out-of-bounds", "index out of bounds"],
      "sink": "UBSan array-bounds: indexing a fixed-bound array past its declared bound.",
      "recipe": "Index field = declared_bound (first out of range). Valid prefix to the access.",
      "fp_guard": "One past the bound; not a huge index."
    },
    "negative-size-param": {
      "label": "Negative-size-param", "sanitizer": "asan",
      "aliases": ["negative-size-param", "negative-size"],
      "sink": "malloc/memcpy/memset called with a size that is negative when signed (wraps huge unsigned).",
      "recipe": "A signed length/count field = -1 (smallest negative) so the size computation passes a negative/huge value to the allocator or copy.",
      "fp_guard": "Use -1, not an arbitrary huge positive (OOM in both builds)."
    },
    "bad-cast": {
      "label": "Bad-cast", "sanitizer": "ubsan",
      "aliases": ["bad-cast", "bad cast", "vptr"],
      "sink": "UBSan vptr / a downcast to the wrong C++ type (type confusion).",
      "recipe": "A valid type-A object with its type tag/discriminant set to B, so a B-typed cast/vtable use runs on an A object.",
      "fp_guard": "Object must be otherwise valid; only the type tag is wrong."
    },
    "bad-free": {
      "label": "Bad-free", "sanitizer": "asan",
      "aliases": ["bad-free", "attempting free on address which was not malloc"],
      "sink": "free() of a pointer that wasn't heap-allocated (stack/global/interior pointer).",
      "recipe": "Drive the branch that frees a non-heap or interior pointer (e.g., a flag selecting a stack-buffer path into a shared cleanup that frees).",
      "fp_guard": "Reach the branch with valid structure; only the branch selector differs."
    },
    "use-after-poison-read": {
      "label": "Use-after-poison READ", "sanitizer": "asan",
      "aliases": ["use-after-poison read", "use-after-poison"],
      "sink": "Read of an ASan-poisoned region (custom redzone / container annotation).",
      "recipe": "Index/offset just past the live (un-poisoned) elements of a manually-poisoned buffer.",
      "fp_guard": "One past the live region only."
    },
    "use-after-poison-write": {
      "label": "Use-after-poison WRITE", "sanitizer": "asan",
      "aliases": ["use-after-poison write"],
      "sink": "Write into an ASan-poisoned region.",
      "recipe": "Write index just past the live elements of the poisoned buffer.",
      "fp_guard": "Minimum margin."
    },
    "stack-use-after-return-read": {
      "label": "Stack-use-after-return READ", "sanitizer": "asan",
      "aliases": ["stack-use-after-return read", "stack-use-after-return"],
      "sink": "A read through a pointer to a local that outlived its function (returned/stored).",
      "recipe": "A path that stashes the address of a local (into a struct/global), then a later VALID path reads it after the function returned.",
      "fp_guard": "Both paths valid; the bug is the lifetime, not malformed bytes."
    },
    "stack-use-after-scope-read": {
      "label": "Stack-use-after-scope READ", "sanitizer": "asan",
      "aliases": ["stack-use-after-scope read", "stack-use-after-scope"],
      "sink": "A read of a local after its lexical scope (block) ended.",
      "recipe": "A branch that takes the address of a block-scoped local, leaves the block, then a valid path reuses that address.",
      "fp_guard": "Trigger via valid control flow; don't corrupt the input."
    },
    "memcpy-param-overlap": {
      "label": "Memcpy-param-overlap", "sanitizer": "asan",
      "aliases": ["memcpy-param-overlap", "memcpy-param-overlap in"],
      "sink": "memcpy() with src and dst regions that overlap.",
      "recipe": "Make a single offset/length field force src and dst into the SAME buffer with overlapping ranges (e.g., copy a region onto itself shifted by < length).",
      "fp_guard": "Keep lengths in-bounds — the bug is the overlap, not an overflow."
    },
    "container-overflow-read": {
      "label": "Container-overflow READ", "sanitizer": "asan",
      "aliases": ["container-overflow read", "container-overflow"],
      "sink": "std::vector/std::string element READ past size() but within capacity (ASan container annotations).",
      "recipe": "Index = size() (one past the live elements, still inside capacity).",
      "fp_guard": "Exactly one past size(); not past capacity (different bug / both builds)."
    },
    "container-overflow-write": {
      "label": "Container-overflow WRITE", "sanitizer": "asan",
      "aliases": ["container-overflow write"],
      "sink": "Write to a container element past size() but within capacity.",
      "recipe": "Write index = size().",
      "fp_guard": "One past size() only."
    },
    "dynamic-stack-buffer-overflow-write": {
      "label": "Dynamic-stack-buffer-overflow WRITE", "sanitizer": "asan",
      "aliases": ["dynamic-stack-buffer-overflow write", "dynamic-stack-buffer-overflow"],
      "sink": "Overflow of a variable-length array (VLA) on the stack sized from an input count.",
      "recipe": "Let `count` size the VLA, then write `count+1` elements (one past). Keep `count` itself honest/small.",
      "fp_guard": "Don't make `count` huge (stack OOM in both builds); the write index exceeds it by one."
    },
    "incorrect-function-pointer-type": {
      "label": "Incorrect-function-pointer-type", "sanitizer": "ubsan",
      "aliases": ["incorrect-function-pointer-type", "function-pointer", "cfi"],
      "sink": "UBSan/CFI: a call through a function pointer whose declared type mismatches the callee.",
      "recipe": "A tag/opcode field selecting a handler/callback whose signature differs from the dispatch site's function-pointer type.",
      "fp_guard": "Pick a real-but-mismatched handler id; keep the rest of the record valid."
    }
  }
}
